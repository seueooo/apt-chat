{
  "name": "apt-chat",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd apps/api && source .venv/bin/activate && uvicorn main:app --reload --port 8000",
    "test": "pnpm -r test",
    "test:api": "cd apps/api && source .venv/bin/activate && pytest",
    "lint": "pnpm --filter web lint && cd apps/api && source .venv/bin/activate && ruff check .",
    "format": "pnpm --filter web format && cd apps/api && source .venv/bin/activate && ruff format .",
    "lint:fix": "pnpm --filter web lint:fix && cd apps/api && source .venv/bin/activate && ruff check --fix ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "lefthook": "^2.1.5"
  }
}
